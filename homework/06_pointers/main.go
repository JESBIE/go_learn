package main

import "fmt"

/*
第 06 次作业：指针（进阶版）

完成要求（必做）：
1) 写函数 increase(n *int)，把值加 1。
2) 写函数 resetIfNegative(n *int)：
   - 如果 n 指向的值 < 0，则改成 0。
3) 在 main 里分别演示：
   - 传值调用（值不变）
   - 传指针调用（值会变）

加分挑战（选做）：
- 为 increase 增加 nil 检查，避免空指针崩溃。
*/

func increase(n *int) {
	// TODO: 修改指针指向的值
}

func resetIfNegative(n *int) {
	// TODO: 小于 0 时重置为 0
}

func main() {
	fmt.Println("TODO: 完成 06_pointers 作业")
}

